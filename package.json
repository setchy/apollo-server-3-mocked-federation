{
  "name": "apollo-server-3-mocked-federation",
  "version": "1.0.0",
  "description": "Apollo Server 3 mocked federation example",
  "main": "index.js",
  "engines": {
    "node": "16"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --watch .env index.ts",
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Setch <@setchy>",
  "license": "ISC",
  "dependencies": {
    "@apollo/gateway": "2.4.0",
    "@faker-js/faker": "^7.6.0",
    "@apollo/server": "4.0.0",
    "dotenv": "16.0.3"
  },
  "devDependencies": {
    "ts-node-dev": "2.0.0"
  }
}